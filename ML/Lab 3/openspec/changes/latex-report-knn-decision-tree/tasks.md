## 1. Asset Extraction

- [x] 1.1 Extract all embedded base64 PNG images from `knn_vs_decision_tree_analysis.ipynb` to `figures/` (`dt_tree_structure.png`, `dt_confusion_matrix.png`, `dt_error_vs_depth.png`, `knn_confusion_matrix.png`, `knn_error_vs_k.png`) and verify all 5 image files are valid.

## 2. LaTeX Document Construction

- [x] 2.1 Assemble `main.tex` with specified preamble, packages, `code`/`output` listing styles, running fancy headers for Kishan Sahu (P26DS017), and department title block. Verify preamble formatting.
- [x] 2.2 Populate Section 1 (Problem Formulation & Dataset Preprocessing) with code listings, outputs, and dataset characteristics. Verify code listing styling.
- [x] 2.3 Populate Section 2 (Decision Tree Classification) with mathematical definitions (Entropy, Information Gain, Gini Impurity), training code, tree diagram figure, confusion matrix figure, and depth error curve. Verify equations and image floats.
- [x] 2.4 Populate Section 3 (K-Nearest Neighbors Classification) with distance metric formulas (Euclidean, Manhattan, Minkowski), training code, confusion matrix figure, and $K$-parameter error curve. Verify equations and image floats.
- [x] 2.5 Populate Section 4 (Comparative Evaluation & Synthesis) with a formal `booktabs` metric comparison table and algorithmic trade-off matrix. Verify table syntax.

## 3. Verification & Validation

- [x] 3.1 Perform syntax validation on `main.tex`, checking balanced delimiters, valid listing environments, graphic references, and character escaping.
