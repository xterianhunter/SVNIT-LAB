## 1. Core Implementation

- [x] 1.1 Implement sequential and OpenMP parallel linear regression with 2D features and 1D label in `linear_regression_omp.c` and verify compilation with `gcc -O3 -fopenmp linear_regression_omp.c -o linear_regression_omp -lm`.
- [x] 1.2 Execute benchmark comparing sequential and parallel execution times across multiple threads, verifying speedup calculation and convergence.
