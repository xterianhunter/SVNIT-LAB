## Why

A standardized LaTeX lab assignment report document is needed to present all Lab 1 dynamic memory allocation C programs (`min_max.c`, `matrix_mult.c`, `dot_product.c`, `merge_sort.c`), their execution outputs, and brief algorithmic/complexity analyses using the provided department styling and header.

## What Changes

- Create `lab1_report.tex` utilizing the provided preamble, typography, listings configuration, and SVNIT header format.
- Embed the source code of all 4 programs using the defined `code` listings style.
- Include execution outputs for each program using the defined `output` listings style.
- Provide concise algorithmic and complexity analyses for each problem.

## Capabilities

### New Capabilities
- `latex-lab-report`: Complete LaTeX lab report file with header, listings, outputs, and analysis.

### Modified Capabilities
<!-- Existing capabilities whose REQUIREMENTS are changing -->

## Impact

- Adds `lab1_report.tex` to the repository root.
- No impact on existing C source files or binaries.
