## 1. Implementation

- [x] 1.1 Create `min_max.c` with dynamic memory allocation, element input, single-pass min/max search, and memory deallocation; verify compilation with `gcc -Wall -Wextra -std=c99 min_max.c -o min_max`

## 2. Verification

- [x] 2.1 Run compiled binary with sample inputs (including negative, zero, and positive numbers) and verify output correctness and clean exit
