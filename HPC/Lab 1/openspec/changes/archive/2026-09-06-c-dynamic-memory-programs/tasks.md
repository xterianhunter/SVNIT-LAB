## 1. Matrix Multiplication

- [x] 1.1 Create `matrix_mult.c` with dynamic 2D array allocation, input, multiplication, and deallocation; verify compilation with `gcc -Wall -Wextra -std=c99 matrix_mult.c -o matrix_mult`

## 2. Vector Dot Product

- [x] 2.1 Create `dot_product.c` with dynamic 1D vector allocation, input, dot product calculation, and deallocation; verify compilation with `gcc -Wall -Wextra -std=c99 dot_product.c -o dot_product`

## 3. Divide-and-Conquer Sort

- [x] 3.1 Create `merge_sort.c` with dynamic array allocation, merge sort algorithm, and deallocation; verify compilation with `gcc -Wall -Wextra -std=c99 merge_sort.c -o merge_sort`

## 4. Verification

- [x] 4.1 Execute `matrix_mult`, `dot_product`, and `merge_sort` with sample test cases to verify numerical correctness and proper exit
