## 1. Table Header Modifications

- [x] 1.1 Update the table header in Table 1 ($500 \times 500$) in `Lab2.tex` to split "Decomposition Method" and "Throughput (GFLOPS)" into two stacked lines using `\shortstack`
- [x] 1.2 Update the table header in Table 2 ($1000 \times 1000$) in `Lab2.tex` to split "Decomposition Method" and "Throughput (GFLOPS)" into two stacked lines using `\shortstack`
- [x] 1.3 Update the table header in Table 3 ($1500 \times 1500$) in `Lab2.tex` to split "Decomposition Method" and "Throughput (GFLOPS)" into two stacked lines using `\shortstack`

## 2. Verification and Compilation

- [x] 2.1 Compile `Lab2.tex` with `pdflatex -interaction=nonstopmode Lab2.tex` and verify exit code 0 without layout regressions
- [x] 2.2 Verify table column widths and multi-line alignment in the generated PDF
