## 1. Environment & Asset Verification

- [x] 1.1 Verify presence of `Filtering-Cameraman.png` and required Python packages (`numpy`, `matplotlib`, `cv2`/`PIL`) by running a brief environment check.

## 2. Notebook Implementation

- [ ] 2.1 Initialize `cvip_lab4.ipynb` with clean markdown headers, image loading, and concise noise addition utilities (Salt-and-Pepper and Gaussian noise).
- [ ] 2.2 Implement concise, vectorized order statistics filters (median, min, max, midpoint, alpha-trimmed mean, and 25th/75th percentiles) using NumPy sliding windows.
- [ ] 2.3 Implement multi-scale evaluation across window sizes ($3 \times 3$, $5 \times 5$, $7 \times 7$) and generate clean comparison plot grids.
- [ ] 2.4 Add concise analytical observations in markdown cells discussing noise reduction trade-offs versus edge and detail preservation.

## 3. Verification

- [ ] 3.1 Run `cvip_lab4.ipynb` head-to-tail to verify that all cells execute cleanly without warnings or errors and render all comparison figures.
