## 1. Setup & Problem 1 (Searching)

- [x] 1.1 Create directory `clean_code/` and implement `clean_code/problem1_searching.py` containing first-principles Linear Search and Binary Search, file-based input loading for sizes (10, 50, 100, 200), 4 positional test cases, and formatted tabular output. Verify execution with `python3 clean_code/problem1_searching.py`.

## 2. Problem 2 (Sorting)

- [x] 2.1 Implement `clean_code/problem2_sorting.py` containing first-principles Bubble Sort (with early exit), Selection Sort, and Insertion Sort across Random, Sorted, and Reverse datasets (sizes 50, 100, 200, 500), comparisons tracking, and formatted tabular output. Verify execution with `python3 clean_code/problem2_sorting.py`.

## 3. Problem 3 (Matrix) & Verification

- [x] 3.1 Implement `clean_code/problem3_matrix.py` containing first-principles Matrix Addition, Matrix Transpose, and Matrix Multiplication across square matrix dimensions (10x10 to 500x500), execution timing, and formatted tabular output. Verify execution with `python3 clean_code/problem3_matrix.py`.
- [x] 3.2 Verify all three scripts in `clean_code/` for line conciseness (~100–150 lines), zero external dependencies, clean formatting, and total suitability for academic lab report inclusion.
