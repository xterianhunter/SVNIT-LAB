## 1. Core Implementation

- [x] 1.1 Implement `check_matrix_stability(matrix)` in `problem4_stable_matrix.py` calculating $D(i, j)$, tracking all violating positions, and identifying the maximum absolute difference without extra matrices.
- [x] 1.2 Add built-in demonstration examples and interactive CLI mode in `problem4_stable_matrix.py`.

## 2. Unit Testing

- [x] 2.1 Create `test_problem4.py` verifying stable matrices, single and multiple violations, maximum difference accuracy, and edge cases ($n < 3$).

## 3. Analysis Documentation

- [x] 3.1 Create `problem4_analysis.md` detailing pseudocode, best/worst case time complexity ($\Theta(n^2)$), auxiliary space ($O(1)$), and explanation of why no additional matrix is required.
