## 1. Core Implementation

- [x] 1.1 Implement `find_dominant_brute_force(arr)` and `find_dominant_boyer_moore(arr)` in `problem6_dominant_element.py`.
- [x] 1.2 Implement demonstration cases from lab sheet and interactive comma-separated integer CLI runner in `problem6_dominant_element.py`.

## 2. Unit Testing

- [x] 2.1 Create `test_problem6.py` testing arrays with dominant elements, arrays with no dominant elements, single elements, even/odd lengths, and negative numbers.

## 3. Analysis Documentation

- [x] 3.1 Create `problem6_analysis.md` documenting pseudocode, step-by-step complexity analysis, and proof that dominant element can be found in $O(n)$ time with $O(1)$ space without additional storage.
