## 1. Core Implementation

- [x] 1.1 Implement `compare_strings(s1, s2)` and `analyze_directory_ordering(names)` in `problem5_nearly_ordered_directory.py` with inversion detection and single-swap validation.
- [x] 1.2 Implement demonstration cases from lab sheet and interactive comma-separated CLI runner in `problem5_nearly_ordered_directory.py`.

## 2. Unit Testing

- [x] 2.1 Create `test_problem5_nearly_ordered.py` testing already sorted, adjacent swap, non-adjacent swap, multiple unfixable violations, duplicates, and edge cases.

## 3. Analysis Documentation

- [x] 3.1 Create `problem5_nearly_ordered_analysis.md` documenting pseudocode, time complexity ($O(n \cdot L)$), auxiliary space ($O(1)$), and mathematical conditions under which a single swap restores ordering.
