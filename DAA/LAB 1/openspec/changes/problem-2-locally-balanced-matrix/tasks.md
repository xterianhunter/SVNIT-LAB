## 1. Core Implementation

- [x] 1.1 Implement `is_locally_balanced(matrix)` in `problem2_locally_balanced_matrix.py` with orthogonal neighbour summing and first row-major violation reporting.
- [x] 1.2 Add built-in demonstration matrices (both balanced and unbalanced) and clean CLI output in `problem2_locally_balanced_matrix.py`.

## 2. Unit Testing

- [x] 2.1 Create `test_problem2.py` covering balanced matrices, unbalanced matrices, corners, edges, and $1 \times 1$ matrices, and verify all tests pass.

## 3. Complexity & Boundary Analysis Documentation

- [x] 3.1 Create `problem2_analysis.md` detailing corner/boundary/interior element handling, pseudocode, and $O(n^2)$ time / $O(1)$ space complexity analysis.
