## 1. Core Algorithm Update

- [x] 1.1 Update `compare_strings` in `problem1_employee_directory.py` with case-insensitive primary comparison and deterministic case tie-breaking, and verify comparison unit tests.
- [x] 1.2 Verify `merge_sort` and `find_duplicates` in `problem1_employee_directory.py` on mixed-case inputs like `['amit', 'Amit', 'rahul', 'Neha', 'Neha', 'Rahul']`.

## 2. Test Suite Updates

- [x] 2.1 Update `test_problem1.py` with test cases for natural dictionary ordering and case tie-breaking, and run test suite to verify 100% pass rate.

## 3. Documentation Updates

- [x] 3.1 Update `problem1_analysis.md` to reflect the two-tier natural dictionary comparison rules and updated pseudocode.
