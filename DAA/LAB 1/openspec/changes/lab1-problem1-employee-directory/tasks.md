## 1. Core Algorithm Implementation

- [x] 1.1 Implement `compare_strings(s1, s2)` adhering strictly to ASCII lexicographical ordering and verify against case-sensitive, prefix, and identical string comparisons.
- [x] 1.2 Implement custom `merge_sort(arr)` without built-in sort functions and verify on unsorted string arrays.
- [x] 1.3 Implement `find_duplicates(sorted_arr)` to detect duplicate names and frequencies in $O(n)$ single-pass time and verify on lists with and without duplicates.

## 2. Input Parsing and CLI Interface

- [x] 2.1 Implement comma-separated user input parsing with whitespace trimming and edge case validation (empty string, single item).
- [x] 2.2 Assemble interactive CLI in `problem1_employee_directory.py` integrating sorting and duplicate reporting, and verify by running sample executions.

## 3. Testing and Theoretical Analysis Documentation

- [x] 3.1 Create automated test runner or test cases covering all edge cases (empty list, single name, all unique, all duplicates, mixed case).
- [x] 3.2 Generate comprehensive documentation in `problem1_analysis.md` containing pseudocode, time/space complexity analysis (best, average, worst), and comparative analysis of duplicate detection with vs. without sorting.
