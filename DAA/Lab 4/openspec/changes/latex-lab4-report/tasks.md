## 1. Benchmark Execution and Visual Asset Generation

- [x] 1.1 Run a Python extraction script to execute the notebook's benchmarking routines for Problem 1 and Problem 2, generating high-resolution PNG plots (`p1_inversion_benchmark.png` and `p2_exponentiation_benchmark.png`) and recording precise timing measurements for tabular inclusion.

## 2. LaTeX Document Implementation

- [x] 2.1 Create `DAA_Lab4_Report.tex` with the exact requested documentclass (12pt, A4, 1in margin), packages, `newtx` fonts, `fancyhdr` student headers (Kishan Sahu, P26DS017), title block for Lab Assignment 4 (Divide and Conquer), and custom `listings` styles for `code` and `output`.
- [x] 2.2 Add Section 1 (Problem 1: Counting Inversions and Sorting Comparison) containing theoretical definitions, complete Python source code listings (Divide-and-Conquer Merge Sort, Brute Force, Quick Sort, Insertion Sort), verification outputs on `[8, 4, 2, 1]`, experimental runtime comparison table, embedded benchmark plot, and Master Theorem recurrence complexity derivation.
- [x] 2.3 Add Section 2 (Problem 2: Fast Exponentiation) containing algorithms for Repeated Multiplication, Naive Recursive, and Divide-and-Conquer, verification on $2^{10}$, experimental runtime comparison table, embedded benchmark plot, iterative binary exponentiation code, and comprehensive responses to all 7 lab questions.

## 3. PDF Compilation and Verification

- [x] 3.1 Verify `DAA_Lab4_Report.tex` environment pairing, syntax integrity, and graphic asset paths via automated validation parser (prepared for portable compilation in Overleaf and standard TeX distributions).
